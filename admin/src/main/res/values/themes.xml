<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Theme.SchoolManagementSystem" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/main_green</item>
        <item name="colorPrimaryVariant">@color/transparent_green</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor" tools:targetApi="l">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>
    <style name="ArrowButtonStyle">
        <item name="android:background">@null</item> <!-- Remove background to make it transparent -->
        <item name="android:padding">8dp</item> <!-- Add padding for touch area -->
    </style>


<!-- Custom ProgressBar  -->
    <style name="CustomProgressBarStyle">
        <item name="android:indeterminateTint">@color/royal_blue</item>
        <item name="android:indeterminateDrawable">@drawable/custom_progress_bar</item>
    </style>
</resources>